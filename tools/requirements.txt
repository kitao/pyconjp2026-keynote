# Python packages used by the scripts under tools/ (capture and asset builds).
# Two other tools are needed as well:
#   Google Chrome - exporting and inspecting the slides (PDF, page images,
#                   measured dimensions). Set CHROME to override its location.
#   ffmpeg        - assembling the videos
#   python3 -m venv .venv && .venv/bin/pip install -r tools/requirements.txt
pyxel>=2.9
pillow>=10
